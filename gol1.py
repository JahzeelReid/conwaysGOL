import tkinter

wn = turtle.Screen()
wn.title("Gol?")
wn.bgcolor("black")
wn.setup(width=500, height=500)
wn.tracer(0)

#points
first = turtle.Turtle()
first.speed(0)
first.shape("square")
first.color("white")
first. size
first.penup()
first.goto(0,0)


#main game loop
while True:
    wn.update()
